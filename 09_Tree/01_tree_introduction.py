# What is Tree??
    # A lot of bracnh
    ## Each node has left and right children
    # Multiple paths
    # BST Average O(log n)

print("Tree = A hierarchical data structure.")
print("Each node can connect to other nodes.")
print("Binary Tree = Each node has at most 2 children.")